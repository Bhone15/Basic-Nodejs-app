extends layouts/main-layout.pug

block content
    h1 Users
    if enteredUser.length > 0
        ul
            each user in users
                li #{user.name}
    else 
        h1 No Users Found! 